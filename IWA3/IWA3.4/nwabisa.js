const firstname = "Nwabisa" //changed the private to const
const surname = "Gabe"  //changed the private to const
const role = "CEO"  //changed the public to const

const display= firstname + " " + surname + " (" + role + ")"  //changed the private to const
document.querySelector('#nwabisa').innerText = display