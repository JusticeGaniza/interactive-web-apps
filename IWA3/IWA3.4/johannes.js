const firstname = "Johannes"  //changed the private to const
const surname = "Potgieter"  //changed the private to const
const role = "Intern"  //changed the public to const

const display= firstname + " " + surname + " (" + role + ")"  //changed the private to const
document.querySelector('#johannes').innerText = display