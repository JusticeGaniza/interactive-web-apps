const firstname = "Alex"  //changed the private to const
const surname = "Naidoo"  //changed the private to const
const role = "Head of Marketing"  //changed the public to const

const display= firstname + " " + surname + " (" + role + ")"  //changed the private to const
document.querySelector('#alex').innerText = display